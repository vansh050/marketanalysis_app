/**
 * designs/default/sdk/TradeExecutionProgress.js
 *
 * Default: re-exports the SDK's built-in TradeExecutionProgress.
 * Props: { progress: { step, totalTrades, completedTrades, currentSymbol }, results[] }
 * Display-only — no action callbacks required.
 */
export { TradeExecutionProgress as default } from '@alphaquark/mobile-sdk';
